# UrbanTicketSystemBackend

## Docker
Both repositories must be in the same directory.
1. Run `ng build` in angular project.
2. Run `mvn clean package` in backend project.
3. Run `docker compose up --build -d` in backend project.